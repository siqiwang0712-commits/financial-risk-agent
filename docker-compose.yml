services:
  api:
    build: {context: ., dockerfile: backend/Dockerfile}
    ports: ["8000:8000"]
    environment: ["FINRISK_LLM_PROVIDER=mock"]
  web:
    build: {context: frontend}
    ports: ["3000:3000"]
    depends_on: [api]

