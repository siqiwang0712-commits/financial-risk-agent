'use client';
import {FormEvent,useState} from 'react';
type Result={company:string;overall_score:number|null;risk_level:string;confidence:number;dimensions:Record<string,{score:number|null;level:string;key_drivers:string[]}>;triggered_rules:{rule_id:string;rationale:string;source_refs:{page:number;source_text:string}[]}[];missing_information:string[];disclaimer:string;extraction?:{review_required:boolean}};
const labels:Record<string,string>={liquidity:'Liquidity',solvency_leverage:'Solvency & Leverage',profitability:'Profitability',cash_flow:'Cash Flow',earnings_quality:'Earnings Quality',accounting:'Accounting',governance_audit:'Governance & Audit',business_going_concern:'Business / Going Concern'};
export default function Home(){const [file,setFile]=useState<File|null>(null);const [company,setCompany]=useState('');const [year,setYear]=useState(new Date().getFullYear()-1);const [result,setResult]=useState<Result|null>(null);const [error,setError]=useState('');const [loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();if(!file)return;setLoading(true);setError('');const body=new FormData();body.append('company',company);body.append('fiscal_year',String(year));body.append('file',file);try{const res=await fetch('http://localhost:8000/api/v1/documents/analyze',{method:'POST',body});const data=await res.json();if(!res.ok)throw new Error(data.detail||'Analysis failed');setResult(data)}catch(err){setError(err instanceof Error?err.message:'Analysis failed')}finally{setLoading(false)}}
return <main><header><div><span className="eyebrow">EVIDENCE-GROUNDED ANALYSIS</span><h1>FinRisk<span>Agent</span></h1></div><div className="status">● Local API</div></header><section className="hero"><div><p className="kicker">Hybrid financial reasoning</p><h2>Risk conclusions you can trace back to the page.</h2><p>Upload an English annual report. Extraction candidates require review; absent evidence never becomes zero risk.</p><form onSubmit={submit}><input aria-label="Company" placeholder="Company name" value={company} onChange={e=>setCompany(e.target.value)} required/><input aria-label="Fiscal year" type="number" value={year} onChange={e=>setYear(Number(e.target.value))} required/><label className="upload"><input type="file" accept="application/pdf" onChange={e=>setFile(e.target.files?.[0]||null)}/><b>{file?.name||'Choose annual report'}</b><small>PDF · maximum 50 MB</small></label><button disabled={!file||loading}>{loading?'Analyzing…':'Analyze report'}</button></form>{error&&<p className="error">{error}</p>}</div>{result&&<aside><div className="score">{result.overall_score}<small>/100</small></div><b>{result.risk_level}</b><p>Confidence coverage {result.confidence}</p><em>{result.extraction?.review_required?'Extraction review required':'Verified inputs'}</em></aside>}</section>{result&&<section><div className="sectionTitle"><h3>Eight-dimensional view</h3><p>{result.company}</p></div><div className="grid">{Object.entries(result.dimensions).map(([key,d],i)=><article key={key}><div className="num">0{i+1}</div><h4>{labels[key]||key}</h4><strong>{d.score??'N/A'} {d.level}</strong><div className="bar"><i style={{width:`${d.score??0}%`}}/></div><p>{d.key_drivers.join(', ')||'Insufficient triggered evidence'}</p></article>)}</div><h3>Evidence-linked rules</h3>{result.triggered_rules.map(r=><article className="rule" key={r.rule_id}><b>{r.rule_id}</b> — {r.rationale}{r.source_refs.map((s,i)=><blockquote key={i}>Page {s.page}: {s.source_text}</blockquote>)}</article>)}</section>}<footer>{result?.disclaimer||'Risk scores are heuristic assessments, not bankruptcy probabilities or investment advice.'}</footer></main>}
